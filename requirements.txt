Flask>=3.0.0
scikit-learn>=1.3.0
joblib>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
pytest>=7.0.0

# Image & Flyer OCR Dependencies
Pillow>=10.0.0
pytesseract>=0.3.10

# NOTE FOR SYSTEM ADMINISTRATORS / DEVELOPERS:
# To enable Flyer OCR text extraction, install the Tesseract-OCR binary engine on your OS:
# - Windows: Download & run installer from https://github.com/UB-Mannheim/tesseract/wiki
# - Linux: sudo apt update && sudo apt install -y tesseract-ocr tesseract-ocr-hin
# - macOS: brew install tesseract tesseract-lang
